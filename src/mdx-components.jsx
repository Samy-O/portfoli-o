// Map HTML elements to custom styled components used inside MDX files.
// Add overrides here as your design system grows.
export const components = {
  a: (props) => <a target="_blank" rel="noopener noreferrer" {...props} />,
}
